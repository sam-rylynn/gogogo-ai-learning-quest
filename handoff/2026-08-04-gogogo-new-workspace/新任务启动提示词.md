请接手 GOGO GO AI 学习平台的新工作区。

第一步只做状态恢复，不要立即修改代码：

1. 阅读 `README-从这里开始.md`。
2. 阅读 `PROJECT-STATE.md` 和 `MEMORY-CONTEXT.md`。
3. 检查实时 `git status --short`、`git rev-parse HEAD` 和 `git diff --stat`。
4. 确认基线应为 `af0c6e3`，其上应只有 5 个本地修改文件。
5. 明确区分线上已发布基线与未验证本地改动。

恢复后先向我报告：当前基线、未提交文件、最新用户验收要求、未验证风险和建议的第一项浏览器验证。不要执行 reset、clean、pull、commit、push 或部署。
